# Authorization Tasks

## Purpose

Use this folder for delivery work on the auth system.

This includes:

- role and permission model rollout
- owner claim behavior
- permission resolution and enforcement mapping
- auth-related docs and validation work

Keep configuration-only schema or loading tasks in `features/configuration/` when they are primarily about persisted shape rather than auth semantics.
